import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { {{camelCase name}}DefaultProps, {{pascalCase name}} } from "./{{camelCase name}}.component";

export default {
  title: "Internal/{{pascalCase name}}",
  component: {{pascalCase name}}
} as ComponentMeta<typeof {{pascalCase name}}>;

const Template: ComponentStory<typeof {{pascalCase name}}> = (args) => <{{pascalCase name}} {...args} />;

export const Default = Template.bind({});
Default.args = {{camelCase name}}DefaultProps;
