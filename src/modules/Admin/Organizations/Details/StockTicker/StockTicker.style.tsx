import styled from "@emotion/styled";

export const SelectWrapper = styled.div`
  position: relative;

  & .close {
    top: 0;
    top: 12px;
    right: 24px;
    position: absolute;
    z-index: 1;
  }
`;
