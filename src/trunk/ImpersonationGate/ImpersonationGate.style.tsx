import styled from "@emotion/styled";
import { Message } from "@q4/nimbus-ui";

export const AccountSwitchedNotificationModal = styled(Message)`
  .nui-message_container {
    width: 478px;
  }

  button.nui-message_exit {
    display: none;
  }
`;
